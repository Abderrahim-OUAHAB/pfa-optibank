import { FilterUserMessagesPipe } from './filter-user-messages.pipe';

describe('FilterUserMessagesPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterUserMessagesPipe();
    expect(pipe).toBeTruthy();
  });
});
